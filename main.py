import radioactive.alias
import assistant
import human_readable as hr
import datetime as dt
import time

# TODO: Play and search radio stations
# TODO: Send and receive WhatsApp messages
# TODO: Access ChatGPT
# TODO: Get the current time
# TODO: Add reminders and timers
# TODO: Play and search radio 
# TODO: Play Youtube Audio




if __name__ == "__main__":
    # print(listener.listen_for_speech("listening to you..."))
    # speech = listener.listen_for_speech("listening to you...", "whisper")
    # print(speech)
    # speaker.speak_text(speech)

    # current_time = dt.datetime.now()
    # current_hour = current_time.hour
    # print(hr.timing(dt.time(13, 31, 40), formal=False))
    # print(hr.timing(dt.time(0, 20, 40), formal=False))
    # print(hr.timing(dt.time(6, 25, 40), formal=False))

    import radioactive


    # assistant.sp.speak_text("We out here walking, like back in the old days.")